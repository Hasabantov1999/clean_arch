name: clean_arch
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  auto_size_text: ^3.0.0
  shared_preferences: ^2.5.2
  flutter_dotenv: ^5.2.1
  sentry_flutter: ^8.9.0
  stacked: ^3.4.4
  snacky: ^0.5.7
  kartal: ^4.1.0
  firebase_core: ^3.11.0
  firebase_messaging: ^15.2.2
  vibration: ^3.1.3

  dio: ^5.8.0+1
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/i18n/

executables:
  clean_arch: bin/clean_arch